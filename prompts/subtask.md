Segment the visible activity into sequential subtasks. Describe each action concisely.
Cover the entire window with no gaps or overlaps, from 0 to window_duration_us.
Use consecutive zero-based indexes. Include idle or unobservable intervals explicitly.
Use the supplied first-pass description as context; correct it if the frames disagree.
