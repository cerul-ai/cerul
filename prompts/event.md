Locate discrete action events. Identify verb, visible objects, actor, and observed
outcome. Use an allowed verb when a vocabulary is supplied. Otherwise use a concise
English verb. Return an empty list if no event is visible.
